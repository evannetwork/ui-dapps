language: node_js
node_js: "12"

install:
  - npm install

jobs:
  include:
    - name: chrome/login
      script: TESTPATTERN=feature/core/login.feature NIGHTWATCH_ENV=chrome npm run test-testnet-specific
