language: node_js
node_js: "12"

install:
  - npm install

jobs:
  include:
    - name: chrome/login
      env: TESTPATTERN=feature/core/login.feature NIGHTWATCH_ENV=chrome


script: npm run test-testnet-specific
